#include <iostream>
#include <list>
#include <random>
#include <ctime>
#include "tank.h"

using namespace std;

generateType1(){

  list<vehicle*> MyVehicles;
  char option = 'z';
  while(option != 'Q'){
    option = menu();
    switch
  }
  list<Tank*>MyTanks;
  list<Tanks*>::iterator titr;
  int Tid;

  Tank * aptr;
  Type1 * bptr;

  aptr = new Tank;
  MyTanks.push_back(aptr);

  bptr = new Type1();
  aptr = bptr;
  MyTanks.push_back(aptr);



}
